# Python - Network #1

This project covers fetching internet resources using Python's urllib and requests packages.

## Tasks

### 0. What's my status? #0
A Python script that fetches `https://intranet.hbtn.io/status` using `urllib`.
